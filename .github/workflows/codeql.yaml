name: "CodeQL Advanced"

on:
  workflow_dispatch: {}

  schedule:
    - cron: "0 6 * * 1" # Weekly on Monday at 06:00 UTC

  push:
    branches:
      - "main"
      - "release-v*.*"

    paths:
      - "!docs/**"
      - "!deploy/**"
      - "!packaging/**"
      - "!**.yaml"
      - "!**.md"
      - "!**.txt"
      - "!**.conf"
      # override previous rules:
      - "go.mod"
      - "go.sum"
      - "Makefile"
      - "**.c"
      - "**.h"
      - "**.go"
      - "**/codeql.yaml"
      - "!.cursor/**"

  pull_request:
    branches:
      - "main"
      - "release-v*.*"

permissions: {}

jobs:
  analyze:
    name: Analyze (${{ matrix.language }})
    runs-on: ${{ vars.UBUNTU_X86_RUNNER_LABEL || (github.repository_owner == 'aquasecurity' && 'ubuntu-2404-4core') }}

    env:
      # "false" is already the default since April 2026, but having the
      # toggle explicit makes it easy to flip for debugging. The repo
      # property "github-codeql-file-coverage-on-prs" can still override
      # this to true (it is evaluated after the env var).
      CODEQL_ACTION_FILE_COVERAGE_ON_PRS: "false"
      # CodeQL manual build-mode traces the compiler on the host: the build
      # must NOT dispatch into the build container (the tracer would see
      # nothing), so force the native toolchain installed below
      NATIVE: "1"

    permissions:
      security-events: write
      packages: read
      actions: read
      contents: read

    strategy:
      fail-fast: false
      matrix:
        language: [c-cpp, go]
        include:
          - language: c-cpp
            build-mode: manual
          - language: go
            build-mode: manual
            goarch: amd64

    steps:
      - name: Checkout repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libelf-dev zlib1g-dev clang make gcc pkgconf file

      - name: Set up Go
        uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7.0.0
        with:
          go-version: "1.26.5"

      - name: Set Go Environment
        if: matrix.language == 'go'
        env:
          GOARCH_VALUE: ${{ matrix.goarch }}
        run: |
          echo "GOOS=linux" >> "${GITHUB_ENV}"
          echo "GOARCH=${GOARCH_VALUE}" >> "${GITHUB_ENV}"

      - name: Update submodules
        run: git submodule update --init --recursive

      - name: Initialize CodeQL
        uses: github/codeql-action/init@e4fba868fa4b1b91e1fdab776edc8cfbe6e9fb81 # v4.37.3
        with:
          languages: ${{ matrix.language }}
          build-mode: ${{ matrix.build-mode }}
          queries: security-extended
          config: |
            paths-ignore:
              - 3rdparty

      - name: Build C/eBPF objects
        if: matrix.language == 'c-cpp'
        run: |
          make bpf

      - name: Build Go
        if: matrix.language == 'go'
        run: |
          make

      - name: Perform CodeQL Analysis
        uses: github/codeql-action/analyze@e4fba868fa4b1b91e1fdab776edc8cfbe6e9fb81 # v4.37.3
        with:
          category: "/language:${{ matrix.language }}"
