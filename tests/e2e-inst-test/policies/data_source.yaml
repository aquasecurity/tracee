name: data_source_test
description: test the containers data source in tracee single binary experience
scope: 
  - container=new
  - comm=ls
defaultActions: 
  - log
rules:
  - event: CONTAINERS_DATA_SOURCE
