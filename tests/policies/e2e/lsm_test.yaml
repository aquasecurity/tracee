apiVersion: tracee.aquasec.com/v1beta1
kind: Policy
metadata:
  name: lsm-test
  annotations:
    description: test tracee's LSM support
spec:
  scope: 
    - comm=lsm_e2e_test
  rules:
    - event: LSM_TEST # e2e signature for tracing
