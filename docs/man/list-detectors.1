.\" Automatically generated by Pandoc 3.2
.\"
.TH "" "" "" "" ""
.PP
\[en] title: TRACEE\-LIST\-DETECTORS section: 1 header: Tracee List
Detectors Command Manual date: 2025/01 \&...
.SS NAME
tracee \f[B]list detectors\f[R] \- List available detectors
.SS SYNOPSIS
tracee \f[B]list detectors\f[R] [paths\&...]
[\-\-json]
.SS DESCRIPTION
The \f[B]list detectors\f[R] command displays all available detectors
from built\-in sources and YAML detector files.
.PP
Detectors analyze events and produce threat detections or derived
events.
.SS ARGUMENTS
.TP
\f[B][paths\&...]\f[R]
Directories or files to search for YAML detectors.
If not specified, uses default paths (/etc/tracee/detectors).
.SS FLAGS
.TP
\f[B]\-\-json\f[R], \f[B]\-j\f[R]
Output in JSON format for scripting.
.SS OUTPUT COLUMNS
.TP
\f[B]ID\f[R]
Unique detector identifier (e.g., TRC\-001, DRV\-001)
.TP
\f[B]Name\f[R]
Detector event name
.TP
\f[B]Severity\f[R]
Threat severity level (info, low, medium, high, critical)
.TP
\f[B]Required Events\f[R]
Events the detector needs to receive
.TP
\f[B]MITRE\f[R]
MITRE ATT&CK technique ID if applicable
.SS EXAMPLES
.IP \[bu] 2
List all detectors from default paths:
.IP
.EX
tracee list detectors
.EE
.IP \[bu] 2
List detectors from a custom directory:
.IP
.EX
tracee list detectors ./my\-detectors
.EE
.IP \[bu] 2
List detectors from multiple directories:
.IP
.EX
tracee list detectors ./dir1 ./dir2
.EE
.IP \[bu] 2
List a single detector file:
.IP
.EX
tracee list detectors ./my\-detector.yaml
.EE
.IP \[bu] 2
List detectors in JSON format:
.IP
.EX
tracee list detectors \-\-json
.EE
.IP \[bu] 2
List detectors and filter with jq:
.IP
.EX
tracee list detectors \-\-json | jq \[aq].[] | select(.severity == \[dq]CRITICAL\[dq])\[aq]
.EE
.SS SEE ALSO
tracee\-list(1), tracee\-list\-events(1), tracee\-list\-policies(1)
