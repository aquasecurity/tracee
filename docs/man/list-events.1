.\" Automatically generated by Pandoc 3.2
.\"
.TH "" "" "" "" ""
.PP
\[en] title: TRACEE\-LIST\-EVENTS section: 1 header: Tracee List Events
Command Manual date: 2025/01 \&...
.SS NAME
tracee \f[B]list events\f[R] \- List traceable events with optional
filtering
.SS SYNOPSIS
tracee \f[B]list events\f[R] [filters\&...]
[\-\-json]
.SS DESCRIPTION
The \f[B]list events\f[R] command displays all events that can be traced
by Tracee, with optional filtering.
.PP
Uses the same filter syntax as \f[B]tracee \-\-events\f[R].
.SS FILTER PATTERNS
.TP
\f[B]eventname\f[R]
Exact event name match (e.g., \f[CR]open\f[R], \f[CR]execve\f[R])
.TP
\f[B]pattern*\f[R]
Wildcard pattern match.
Supports prefix (\f[CR]open*\f[R]), suffix (\f[CR]*write\f[R]), or
contains (\f[CR]*file*\f[R])
.TP
\f[B]tag=TAG\f[R]
Filter by tag/set (e.g., \f[CR]tag=fs\f[R], \f[CR]tag=network\f[R],
\f[CR]tag=syscalls\f[R])
.TP
\f[B]tag=TAG1,TAG2\f[R]
Filter by multiple tags with OR logic (e.g., \f[CR]tag=fs,network\f[R])
.TP
\f[B]type=TYPE\f[R]
Filter by event type.
Valid values: \f[CR]syscall\f[R], \f[CR]detector\f[R],
\f[CR]network\f[R]
.TP
\f[B]threat.severity=SEVERITY\f[R]
Filter by threat severity.
Valid values: \f[CR]info\f[R], \f[CR]low\f[R], \f[CR]medium\f[R],
\f[CR]high\f[R], \f[CR]critical\f[R]
.TP
\f[B]threat.mitre.technique=ID\f[R]
Filter by MITRE ATT&CK technique ID (e.g.,
\f[CR]threat.mitre.technique=T1055\f[R])
.TP
\f[B]threat.mitre.tactic=NAME\f[R]
Filter by MITRE ATT&CK tactic name (e.g.,
\f[CR]threat.mitre.tactic=Execution\f[R])
.SS OUTPUT FLAGS
.TP
\f[B]\-\-json\f[R], \f[B]\-j\f[R]
Output in JSON format for scripting.
.SS FILTER SEMANTICS
Multiple filter arguments are combined with \f[B]AND\f[R] logic:
.IP
.EX
tracee list events tag=fs threat.severity=high
# Events must have \[aq]fs\[aq] tag AND high severity
.EE
.PP
Comma\-separated values within a filter are combined with \f[B]OR\f[R]
logic:
.IP
.EX
tracee list events tag=fs,network
# Events with \[aq]fs\[aq] OR \[aq]network\[aq] tag
.EE
.SS EXAMPLES
.IP \[bu] 2
List all events:
.IP
.EX
tracee list events
.EE
.IP \[bu] 2
List event by exact name:
.IP
.EX
tracee list events open
.EE
.IP \[bu] 2
List events matching a pattern:
.IP
.EX
tracee list events \[aq]open*\[aq]
tracee list events \[aq]*write*\[aq]
.EE
.IP \[bu] 2
List events with a specific tag:
.IP
.EX
tracee list events tag=fs
tracee list events tag=syscalls
.EE
.IP \[bu] 2
List events with multiple tags (OR):
.IP
.EX
tracee list events tag=fs,network
.EE
.IP \[bu] 2
List events with multiple tags (AND):
.IP
.EX
tracee list events tag=fs tag=proc
.EE
.IP \[bu] 2
List syscall events only:
.IP
.EX
tracee list events type=syscall
.EE
.IP \[bu] 2
List detector events only:
.IP
.EX
tracee list events type=detector
.EE
.IP \[bu] 2
List events by threat severity:
.IP
.EX
tracee list events threat.severity=critical
tracee list events threat.severity=high,critical
.EE
.IP \[bu] 2
List events by MITRE technique:
.IP
.EX
tracee list events threat.mitre.technique=T1055
.EE
.IP \[bu] 2
List events by MITRE tactic:
.IP
.EX
tracee list events threat.mitre.tactic=Execution
.EE
.IP \[bu] 2
Combine multiple filters (AND):
.IP
.EX
tracee list events tag=fs threat.severity=high
tracee list events type=detector threat.mitre.tactic=Persistence
.EE
.IP \[bu] 2
Output in JSON format:
.IP
.EX
tracee list events \-\-json
tracee list events tag=fs \-\-json | jq \[aq].[] | .name\[aq]
.EE
.SS SEE ALSO
tracee\-list(1), tracee\-list\-detectors(1), tracee\-list\-policies(1)
