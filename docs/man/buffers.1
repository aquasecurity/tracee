.\" Automatically generated by Pandoc 3.2
.\"
.TH "TRACEE\-BUFFERS" "1" "2025/12" "" "Tracee Buffers Flag Manual"
.SS NAME
tracee \f[B]\-\-buffers\f[R] \- Set kernel/user buffers size
.SS SYNOPSIS
tracee \f[B]\-\-buffers\f[R] [kernel.events=<size> |
kernel.artifacts=<size> | kernel.control\-plane=<size> |
pipeline=<size>] \&...
[\f[B]\-\-buffers\f[R] [kernel.events=<size> | kernel.artifacts=<size> |
kernel.control\-plane=<size> | pipeline=<size>] \&...]
.SS DESCRIPTION
The \f[B]\-\-buffers\f[R] flag allows you to configure the sizes of the
buffers used by tracee.
.PP
Buffer sizes for perf ring buffers (kernel.events, kernel.artifacts,
kernel.control\-plane) are specified in pages.
The pipeline buffer size is specified in event objects.
The default size for perf ring buffers is 1024 pages.
The default size for the pipeline buffer is 1000 event objects.
.PP
Possible buffer options:
.IP \[bu] 2
\f[B]kernel.events=<size>\f[R]: Sets the size, in pages, of the internal
perf ring buffer used to submit events from the kernel.
.IP \[bu] 2
\f[B]kernel.artifacts=<size>\f[R]: Sets the size, in pages, of the
internal perf ring buffer used to send artifacts from the kernel.
.IP \[bu] 2
\f[B]kernel.control\-plane=<size>\f[R]: Sets the size, in pages, of the
internal perf ring buffer used to submit events from the control plane.
.IP \[bu] 2
\f[B]pipeline=<size>\f[R]: Sets the size, in event objects, of each
pipeline stage\[cq]s output channel.
.SS EXAMPLES
.IP \[bu] 2
To set the kernel events buffer size to 2048 pages, use the following
flag:
.RS 2
.IP
.EX
\-\-buffers kernel.events=2048
.EE
.RE
.IP \[bu] 2
To set multiple buffer sizes at once, use multiple flags:
.RS 2
.IP
.EX
\-\-buffers kernel.events=2048 \-\-buffers pipeline=20000
.EE
.RE
.IP \[bu] 2
To set all buffer sizes in a single command, use the following flags:
.RS 2
.IP
.EX
\-\-buffers kernel.events=2048 \-\-buffers kernel.artifacts=1024 \-\-buffers kernel.control\-plane=512 \-\-buffers pipeline=20000
.EE
.RE
.IP \[bu] 2
To only override the kernel.artifacts buffer size while keeping other
buffers at their default values, use the following flag:
.RS 2
.IP
.EX
\-\-buffers kernel.artifacts=2048
.EE
.RE
